{
  "name": "mass-effect",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A small, three-dimensional orbital sandbox. No runtime dependencies.",
  "scripts": {
    "start": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "wrangler": "4.131.1"
  }
}
